import time
import gymnax


def speed_gymnax_random(env_name, num_env_steps, rng):
    return


def speed_gymnax_network(env_name, num_env_steps, rng, model, model_params):
    return


def speed_numpy_random(env_name, num_env_steps):
    return


def speed_numpy_network(env_name, num_env_steps, rng, model, model_params):
    return
